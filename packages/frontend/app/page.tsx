// L6 home — the conversation surface: the spiral as user bubbles → the glass dot
// revealing → validate-first → subjective → objective (the real counts) → the
// DELTA as the catch → the provider report. Runs the REAL Grok reflection from
// @dot/backend via /api/run. The thread is the product (design/05-NEW-BRAND).
import { ThreadSurface } from '../components/ThreadSurface';

export default function Page() {
  return <ThreadSurface />;
}
