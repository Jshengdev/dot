# reference/ — deep planning docs

Drop full planning docs here as they're written (PRD, frontend spec, build scope, validation, etc.) — the
long-form companions to the canonical `docs/` set. The short docs in `docs/` are the read-first map; these are
the drill-down.

> ⏳ **MISSION TODO:** populate once the mission (`docs/SCOPE-LOCK.md`) is locked. Until then this is a
> placeholder so the directory exists.
