# SPONSORS — each sponsor's seam + minimal integration + where it shows on screen.

> ⏳ **MISSION TODO (Johnny fills the sponsor list).** The *rule* is locked; the *names* aren't. Focus on
> ABILITIES, not marketing. Each sponsor must be a **real seam in the node graph, named on screen** at its step.
> Fill this once the journey (`docs/ARCHITECTURE.md`) is locked.

## The locked rule (independent of which sponsors)

- **Every CORE sponsor is on the one journey line** (`docs/SCOPE-LOCK.md`) and **named on screen** at its step.
- **Don't add a node/step just to claim a badge.** If a sponsor doesn't sit on the line, it's CUT (or "wire if
  time" at the very end, only if free + on-path).
- Each sponsor gets the **minimal** integration that makes its seam real — one wrapper, one call, not a toolset.

## The per-sponsor table (⏳ MISSION TODO)

| Sponsor | The ability we use | The seam (which node) | Minimal integration | Where it shows on screen |
|---|---|---|---|---|
| ⏳ TODO | ⏳ TODO | ⏳ TODO | ⏳ TODO | ⏳ TODO |
| ⏳ TODO | ⏳ TODO | ⏳ TODO | ⏳ TODO | ⏳ TODO |

## WIRE vs WIRE-IF-TIME vs SKIP (⏳ MISSION TODO)

- **WIRE (core):** ⏳ TODO — the sponsors on the line, required for the demo.
- **WIRE IF TIME:** ⏳ TODO — free + on-path, added only at the end.
- **SKIP:** ⏳ TODO — anything off the line, paid-and-not-needed, or slow. Name them so they don't creep back in.

## UNVERIFIED — confirm before relying

> ⏳ **MISSION TODO:** list every sponsor API shape you're assuming but haven't called live (response field
> names, exact endpoints, free-credit limits). Make ONE live call to pin each before building on it. (The
> reference repo got bitten by assumed response shapes — verify first.)
