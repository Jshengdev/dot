# DEMO-SCRIPT — the judge-facing 3 minutes (= the demo path).

> The demo path is sacred. This exact sequence gets polish + error handling. Nothing else does.
> ⏳ **MISSION TODO (Johnny fills the beats).** The *structure* of a winning demo is locked below; the content
> is the mission.

## One-breath pitch

> ⏳ **MISSION TODO:** the one sentence that explains DOT to a judge walking by. (Must match the success-test
> sentence in `docs/SCOPE-LOCK.md`.) If you can't say it in one breath, the scope isn't locked yet.

## The beats (one run, live — ~6 beats)

1. **[0:00] The screen.** A calm white room, the conversation surface at rest — clean-blue, breathing.
   (Design: `design/05-NEW-BRAND.md`.)
2. **[0:20] Run.** ⏳ TODO: the user gives DOT its one input. The first node lights; the first rich bubble lands.
3. **[0:40] It works.** ⏳ TODO: the confident first result appears (reads great).
4. **[0:55] THE MOMENT.** ⏳ **MISSION TODO (M6):** the single beat the whole demo is built around — the thing
   the judge walks away remembering. Mark exactly what fires, what turns on screen, and why it lands. This beat
   gets the most polish of anything in the repo.
5. **[1:15] The resolution.** ⏳ TODO: the loop/fix/proof resolves; the surface updates (blur-up swap, a stat
   count-up, a chart bubble).
6. **[1:40] The human's yes → out.** ⏳ TODO: a human confirms (hold-to-confirm / gate-stamp; confetti ONLY on
   the human's yes per `design/`). The output blooms as rich bubbles. "The human takes it from here."
7. **[2:10] (if time) it scales.** Cached inputs flow through; one shows a FAILED badge (we fail loud).
8. **[2:40] Close.** ⏳ TODO: the one closing line that names why DOT matters and why a better model alone
   wouldn't solve it.

## Fallback (LOCKED discipline)

- Run ONE input live; keep 2 cached inputs in `data/` if the live path fails on stage.
- Record a backup video before the freeze (`docs/BUILD-LOOP.md` S4).
- A failure on stage renders a visible FAILED badge — never a silent fake. We fail loud, on brand.
